PORT=8080

# Database Configuration - Use environment variables for sensitive data
DB_USER=${DB_USER}
DB_PASSWORD=${DB_PASSWORD}
DB_HOST=${DB_HOST}
DB_URI=mongodb+srv://${DB_USER}:${DB_PASSWORD}@${DB_HOST}/?appName=Foodapp

# Environment
NODE_ENV=development
